تمام 👌
سأكتب لك الآن موجّه (برومبت) جاهز بنفس أسلوب الموجّه السابق (PRO V6)، لكن مخصّص للمؤشّر الجديد FT Concept v2.2 مع ماسح Alert Bias Up / Alert Bias Down على Binance USDT-M عبر CCXT، مع فلترة الإشارات القديمة وعدة فريمات، وعدد شموع مطابق للمؤشر. 

يمكنك نسخ النصّ كاملًا واستخدامه كموجّه لأي نموذج لتحويل الكود إلى بايثون، أو استخدامه معي في رسالة جديدة وأقول لك بعدها الكود مباشرة حسبه.


---

الموجّه المقترح لتحويل FT Concept v2.2 إلى Python + Scanner

> [المبدأ الحاكم]

الهدف غير قابل للتفاوض: تحويل المنطق الكامل لمؤشر TradingView (FT Concept v2.2) من أول سطر إلى آخر سطر، بدون حذف أو اختصار أو تبسيط لأي منطق يؤثّر على النتائج، إلى سكربت Python واحد يعمل على بيانات Binance USDT-M Futures عبر CCXT، ويُنتِج نفس الحالات والإشارات (خصوصًا Alert Bias Up / Alert Bias Down) بدقة مستهدفة 99.99% قدر الإمكان.

الاتساق 1:1: كل ما في الكود الأصلي من:
• مدخلات (input.)
• ثوابت (const)
• متغيّرات (var)
• مصفوفات (array.)
• Types / Structs / Methods
• منطق حسابي/شرطي
• request.security للتايم فريمات الأعلى
• alertcondition / alert / label / line / box
يجب أن يُعاد تمثيله منطقيًا في بايثون، مع الحفاظ على المعاني والحالات التي تؤثر على الإشارات، حتى لو لم يتم رسم أي شيء على الشاشة.



---

[الدور]

أنت نموذج ذكاء اصطناعي/مُبرمج Python متخصّص في:
• تحليل وتحويل Pine Script v5 إلى Python 3.
• ربط بايثون مع Binance USDT-M Futures باستخدام CCXT.
• بناء ماسح Scanner لكل الأزواج مع منطق إشارات مطابق للمؤشر الأصلي.

تعمل كمحوِّل كود Methodical Code Transpiler، وليس كملخّص؛ ممنوع حذف أو تبسيط أي منطق يؤثر على النتائج، حتى لو كان معقّدًا (مثل HTF FVG, CISD, iFVG … إلخ).



---

[بيئة التنفيذ المطلوبة]

لغة: Python 3.x فقط.

المكتبات المسموح بها:
• ccxt  (إلزامي لجلب بيانات Binance USDT-M Futures)
• numpy
• pandas
• مكتبة تحليل فني اختيارية (ta أو ta-lib) أو إعادة كتابة المعادلات يدويًا عند الحاجة.

بيئة التشغيل المستهدفة:
• محرّر Python على أندرويد (مثل Pydroid3) في نمط console.
• بدون Jupyter Notebook، بدون GUI ثقيل.
• الإخراج عبر print / logging إلى شاشة المحرّر فقط.



---

[المطلوب من السكربت – نظرة عامة]
حوّل كود المؤشر FT Concept v2.2 بالكامل إلى سكربت Python يقوم بـ:

1. إعادة تنفيذ كل المنطق الحسابي للمؤشر، بما في ذلك على الأقل:

نظام الـ Bias (المبني على: _bias, __arrLow, __arrHigh, _continueFlag, FVG شرط __isFvgUp / __isFvgDown, العدّادات countBull / countBear, وحالات bullBias / bearBias).

منطق HTF FVGs (الـ Types: Settings, Imbalance_Settings, Imbalance, ImbalanceStructure, Helper + methods: AddZone, AddImbalance, CheckMitigated, AdjustMargins, FindImbalance, Process، مع request.security لفريمات HTF).

منطق LuxAlgo Imbalance / FVG (الدوال: imbalance_detection_lux, bull_filled_lux, bear_filled_lux، ومتغيرات bull_fvg, bear_fvg, bull_fvg_filled, bear_fvg_filled).

نظام SB CISD Detector (الـ types: bin, swing، والمصفوفات arrBull, arrBear, swingsH, swingsL، ومنطق bullishCISD / bearishCISD).

منطق iFVG Detector (المتغيرات ig_*, bullishFVG, bearishFVG, validated, والـ box.new الخاص بها).



2. التركيز الخاص على إعادة بناء منطق تنبيهات:

Alert Bias Up  (عندما تكون bullBias = True و biasUpAlert = True على إغلاق الشمعة).

Alert Bias Down (عندما تكون bearBias = True و biasDownAlert = True على إغلاق الشمعة).
بحيث ينتج السكربت نفس التوقيت تقريبًا لنفس الشمعة التي يظهر فيها التنبيه في TradingView.



3. استخدام CCXT لجلب OHLCV لجميع أزواج Binance USDT-M Futures، على الأطر الزمنية التي يحددها المستخدم (قابلة للتخصيص).


4. فحص كل زوج، وتطبيق منطق المؤشر عليه، ثم توليد تنبيهات نصّية على الشاشة عند تحقق:

Bias Up Alert

Bias Down Alert
(مع إمكانية توسيع السكربت لاحقًا لطباعة تنبيهات Bullish/Bearish FVG, CISD, iFVG عند الحاجة).



5. توفير إعدادات Config في أعلى الكود للتحكم في:

قائمة الأزواج المراد مسحها.

خيار use_all_symbols لأخذ كل أزواج USDT-M تلقائيًا أو استخدام symbols_whitelist.

قائمة الفريمات المراد فحصها (مثل: ["5m", "15m", "1h"]).

عدد الشموع التاريخية المستخدمة بحيث لا يقل عن max_bars_back في المؤشر (هنا 500)، وقابل للزيادة.

إعدادات فلترة الإشارات القديمة (لإظهار الإشارات الحديثة فقط).



6. أن يكون السكربت قابلاً للتشغيل مباشرة من محرّر بايثون على أندرويد:

باستخدام بنية if __name__ == "__main__": main().





---

[تفاصيل التحويل من Pine إلى Python]

1. احترام البنية العامة للمؤشر

ابدأ من تعريف indicator("FT Concept v2.2", ...) وتعامل معه كـ "وصف" وليس كوظيفة تنفيذية، لكن:
• استخرج max_bars_back, max_lines_count, max_boxes_count, max_labels_count كمؤشرات على احتياجات الذاكرة.



2. المدخلات (Inputs)

حوّل جميع:
• input.bool (مثل: showLine, biasUpAlert, biasDownAlert, showLabelBias, alert_fvg_bull, alert_fvg_bear, alert_cisd_bull, alert_cisd_bear, alert_ifvg_bull, alert_ifvg_bear, …)
• input.int (مثل: length, Minimum_Sequence_Length, Maximum_Sequence_Length, lineWidth, …)
• input.float (مثل: fvg_gapwidth)
• input.string (مثل: Tracking_Method, textSize, alert_tf, fvg_method, …)
• input.timeframe (مثل: htf_1, htf_2, htf_3)
• input.color (مثل: bull_fvg_css, bear_fvg_css, cBull_sb, cBear_sb, …)
إلى متغيّرات Python داخل قسم CONFIG، بنفس الأسماء والقيم الافتراضية قدر الإمكان، مع تعليقات توضّح الغرض.



3. البيانات من TradingView إلى Python

حوّل كل request.security(syminfo.tickerid, timeframe, series...) إلى منطق Python يكافئ:
• إما استدعاء fetch_ohlcv بفريم HTF المطلوب، وبناء DataFrame منفصل للفريم الأعلى.
• أو إعادة تشكيل الـ Series من الفريم الرئيسي (لو أردت لكن الأفضل جلبها مباشرة بنفس الفريم كما في TV).

استبدل syminfo.tickerid برمز Binance المقابل في CCXT مثل "BTC/USDT" لأسواق USDT-M (باستخدام exchange.load_markets() للربط الصحيح).

استبدل دوال ta.* في Pine (مثل ta.atr, ta.pivothigh, ta.pivotlow, وغيرها) بدوال Python:
• إمّا من مكتبة ta / ta-lib
• أو معادلات مكتوبة يدويًا تحافظ على نفس الـ indexing.



4. المتغيّرات والحالة (State)

حافظ على نفس منطق:
• var arrays مثل: __arrLow, __arrHigh, _arrLineHigh, _arrLineLow, _arrLable, FVGs_1/2/3, arrBull, arrBear, swingsH, swingsL.
• المتغيّرات var bool و var int مثل: _continueFlag, _bias, bullBias, bearBias, countBull, countBear, trend, إلخ…

في بايثون، استخدم:
• كائنات / Classes (مثلاً Class لـ ImbalanceStructure و Imbalance و Helper تحاكي الـ types في Pine).
• أو قواميس وقوائم مع المحافظة على المحتوى.

منطق bar_index يجب أن يُعاد تمثيله برقم الصف في DataFrame (index من 0 → n-1، مع اعتبار آخر صف هو آخر شمعة متاحة).



5. التعامل مع الرسم (lines / labels / boxes / tables)

الرسومات البحتة (مثل الألوان، نوع الخط) يمكن إهمالها من ناحية الرسم الفعلي، لكن:
• أي Box أو Line أو Label يدخل في منطق حسابي أو شرطي (كـ “تمت Mitigation” أو “Break of Structure”) يجب تمثيله كهيكل بيانات في Python يحتفظ بالمعلومات اللازمة.

الـ Dashboard / Table في آخر الكود (إعلان التليجرام) يمكن تجاهله لأنه لا يؤثر على إشارات التداول.



6. alertcondition / alert

كل alertcondition(*) وكل استدعاء alert(...) في Pine يجب أن يتحول إلى منطق واضح في Python:
• يتحول إلى Boolean Series (مثل: bias_up_event, bias_down_event, bull_fvg_event, …).
• عند تحقق الشرط على شمعة معينة ضمن نافذة الفحص، يقوم الـ Scanner بطباعة سطر في الـ console.

على وجه الخصوص، يجب أن ينتج السكربت إشعارات نصية على الشاشة عندما تتولد الإشارات:
• Alert Bias Up   (من منطق bullBias + biasUpAlert).
• Alert Bias Down (من منطق bearBias + biasDownAlert).

صيغة الإخراج المقترحة لكل إشارة:
SYMBOL, TIMEFRAME, SIGNAL_NAME, PRICE, TIME
مثال:
BTCUSDT, 15m, Bias Up, 68000.0, 2025-01-01 12:30:00





---

[ماسح Binance USDT-M Futures عبر CCXT]

1. إعداد CCXT

في قسم CONFIG:

EXCHANGE_ID = "binanceusdm"
USE_ALL_SYMBOLS = True
SYMBOLS_WHITELIST = ["BTC/USDT", "ETH/USDT"]  # إذا USE_ALL_SYMBOLS = False
SCAN_TIMEFRAMES = ["5m", "15m", "1h"]        # فريمات الفحص القابلة للتخصيص

كود التهيئة:

import ccxt
def init_exchange():
    exchange = ccxt.binanceusdm()
    exchange.load_markets()
    return exchange

عند USE_ALL_SYMBOLS = True:

symbols = [s for s in markets.keys() if s.endswith("/USDT")]



2. جلب البيانات (OHLCV)

لكل رمز، ولكل فريم من SCAN_TIMEFRAMES:

ohlcv = exchange.fetch_ohlcv(symbol, timeframe, limit=MAX_BARS)

تحويل النتائج إلى DataFrame بأعمدة: timestamp, open, high, low, close, volume مرتبة من الأقدم إلى الأحدث.



3. حلقة المسح (Scanner Loop)

دالة مثل:

def process_symbol(symbol, timeframe, df, config):
    # تعيد تنفيذ منطق FT Concept v2.2 على DataFrame
    # تحسب Bias + باقي الأنظمة
    # تُرجِع قائمة بالإشارات الحديثة (Bias Up / Bias Down)

دالة رئيسية:

def main():
    exchange = init_exchange()
    symbols = get_selected_symbols(exchange)
    while True:
        for symbol in symbols:
            for timeframe in SCAN_TIMEFRAMES:
                df = fetch_ohlcv_dataframe(exchange, symbol, timeframe, MAX_BARS)
                signals = process_symbol(symbol, timeframe, df, CONFIG)
                print_new_signals(signals)
        time.sleep(SCAN_INTERVAL_SECONDS)

يجب أن يكون SCAN_INTERVAL_SECONDS قابلًا للتخصيص في CONFIG.





---

[فلترة الأحداث القديمة]

في أعلى السكربت أضف إعدادات مثل:

MAX_HISTORY_BARS = 500      # مطابق أو أكبر من max_bars_back في Pine
ALERT_LOOKBACK_BARS = 3     # لا تطبع إلا الإشارات التي ظهرت خلال آخر N شموع
ALERT_LOOKBACK_MINUTES = 60 # اختيارية، تصفية إضافية بحسب الزمن المطلق

عندما تحسب إشارة Bias Up/Down على شمعة index معيّن:
• لا تطبع الإشارة إذا كانت الشمعة أقدم من ALERT_LOOKBACK_BARS عن آخر شمعة في الـ DataFrame.
• وإذا كان ALERT_LOOKBACK_MINUTES مفعّلًا، تأكد أن now - candle_time <= ALERT_LOOKBACK_MINUTES.



---

[هيكلة الكود في Python]

1. CONFIG

إعدادات Binance / CCXT (API keys إن لزم، أو بيانات عامة فقط).

إعدادات الفريمات، عدد الشموع، فلترة الإشارات، قائمة الأزواج.

جميع قيم المدخلات (inputs) المأخوذة من FT Concept v2.2.



2. DATA & EXCHANGE HELPERS

دوال:
• init_exchange()
• fetch_ohlcv(symbol, timeframe, limit)
• prepare_dataframe(ohlcv)



3. INDICATOR CORE LOGIC (FT Concept v2.2)

دوال أو Classes مكافئة لمنطق:
• ChoCH / Bias System (الأقسام الأولى حتى قسم Bias).
• HTF FVG Structures + Methods.
• LuxAlgo Imbalance / FVG Logic.
• SB CISD Detector.
• iFVG Detector.

كل دالة تستقبل DataFrame أو Series وتضيف أعمدة جديدة تمثل قيم المؤشرات.



4. SIGNAL ENGINE

دالة تجمع الشروط وتحدّد:
• متى تتحقق Bias Up Alert على شمعة معينة.
• متى تتحقق Bias Down Alert.

تُرجِع List أو Dict بالإشارات (symbol, timeframe, signal_name, price, time).



5. SCANNER

process_symbol(symbol, timeframe, df) يقوم بـ:
• حساب كل المؤشرات اللازمة.
• استخراج الإشارات الحديثة وفق فلترة ALERT_LOOKBACK_*.
• طباعة سطور الإشعارات في الـ console.



6. ENTRY POINT

في نهاية الملف:

if __name__ == "__main__":
    main()





---

[معايير الجودة المطلوبة]

التطابق:
• حافظ على نفس أسماء المتغيرات والجذور قدر الإمكان (مثلاً _bias, bullBias, bearBias, __arrLow, __arrHigh…) مع تكييف بسيط لـ snake_case عند الحاجة في بايثون.
• لا تحذف أي جزء من المنطق الحسابي الذي يمكن نقله إلى Python.

الوضوح:
• استخدم تعليقات إنجليزية قصيرة توضّح كل جزء، مثل:

# Re-implementation of FT Concept v2.2 Bias System

# HTF FVG imbalance structures

# Bias Up / Bias Down signal conditions


الاستقرار:
• تجنّب async أو multi-threading، الكود التسلسلي كافٍ.
• أضف معالجة أخطاء بسيطة عند استدعاء CCXT (استثناءات الشبكة / rate limit) مع إعادة المحاولة أو تخطي الرمز عند الفشل.



---

[
